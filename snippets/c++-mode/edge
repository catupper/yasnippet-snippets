# -*- mode: snippet -*-
# name: Edge
# key: /edge
# group: Graph
# --


class Edge{
public:
  int from, to, rev;
  Int cost, lim;
  Edge(int from, int to, Int cost, int lim = -1, Int rev = -1):
    from(from),to(to),cost(cost),lim(lim),rev(rev){}
};
